# Modules

> OKF reserved-style directory index — no frontmatter, progressive-disclosure link list.
> Unlike `tasks/` and `journal/`, this directory is not auto-maintained by `/task-new` or
> `/task-close` — module files are created and synced only via `/module-sync` (manual,
> see `.agents/workflows/module-sync.md`). Update this list by hand when a module file is
> added, renamed, or deprecated.

# Active

*(no module documents yet)*

# Deprecated

*(none)*
